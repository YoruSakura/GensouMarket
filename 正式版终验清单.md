# GensouMarket 正式版终验清单

更新时间：2026-04-08

当前状态：`1.0.0` 正式版，人工终验完成，项目已结档

## 1. 扫描结论

- 当前仓库为双模块项目：
  - `GensouMarket-plugin`：Paper 业务端插件
  - `GensouMarket-velocity`：Velocity 代理端插件
- 当前源码规模：
  - Paper 端 `43` 个 Java 文件
  - Velocity 端 `11` 个 Java 文件
  - 总计约 `8809` 行源码
- 已实现的功能面：
  - 全球市场
  - 拍卖行
  - 服务器商店
  - 回收站
  - 邮箱系统
  - 面对面交易
  - 跨服桥接与消息转发
- 构建状态：
  - 根目录 `./gradlew build` 已通过
  - 两个模块均可打包
  - 当前无自动化测试，Gradle 显示 `test NO-SOURCE`

## 2. 必须人工确认项

以下项目不应混入普通测试项，必须先由人工决定“修”还是“接受现状”。

### 2.1 阻塞级问题

- [x] 市场购买存在状态悬空风险：
  - 先原子改为 `SOLD`
  - 再回主线程扣钱/发物
  - 若买家在此间下线，流程直接结束，可能留下已售出但未完成交付的状态

- [x] 拍卖出价存在状态悬空风险：
  - 先乐观锁更新当前价格与最高出价者
  - 再回主线程扣款
  - 若竞拍者在此间下线，流程直接结束

- [x] 拍卖出价失败回滚方式需确认：
  - 当前扣款失败时通过普通 `updateAuction(...)` 回滚
  - 这不是条件更新
  - 理论上可能覆盖其他服或后续已成功的新出价

- [x] 市场下架存在返还缺口风险：
  - 先改状态
  - 再回主线程返还物品
  - 若玩家在此间下线，可能出现已取消但未返还

- [x] 拍卖取消存在返还缺口风险：
  - 先改状态
  - 再回主线程退还竞拍金额与物品
  - 若卖家在此间下线，可能出现已取消但未完成返还

### 2.2 一致性与发布决策项

- [ ] 跨服 GUI 视图注册路径不统一是否需要修改：
  - 从主菜单进入市场/拍卖会注册跨服视图
  - 通过命令 `/gmarket search`、`/gmarket auction list` 打开的界面未注册
  - 翻页与部分返回路径也未重新注册
  - 需人工决定是否统一为“所有入口都可跨服刷新”

- [ ] `Velocity插件架构规划.md` 已明显过期是否需要更新：
  - 文档仍写 Velocity 端几乎为空
  - 文档仍写当前没有任何跨服桥接实现
  - 与现状不一致

- [ ] 是否补充根目录 `.gitignore`：
  - 当前仓库无 `.gitignore`
  - `.gradle/`、`build/`、模块构建产物会污染工作区
  - 若要正式归档，建议先明确仓库清洁策略

- [ ] 是否统一正式版版本号：
  - Plugin 当前为 `dev-2.2`
  - Velocity 当前为 `dev-1.1`
  - 需人工决定是否改为正式版号，例如 `1.0.0`

- [ ] 是否继续使用 `SNAPSHOT` API 依赖：
  - Paper API 为 `1.21.4-R0.1-SNAPSHOT`
  - Velocity API 为 `3.4.0-SNAPSHOT`
  - 需人工决定是否在正式版前切换到稳定依赖

- [ ] 是否保留当前文档中的“Phase 已完成”表述：
  - `Claude.md` 中当前状态与代码基本一致
  - 但若准备正式发布，是否要保留开发阶段表述，需要人工拍板

## 3. 最终人工终验清单

以下清单全部通过后，项目才可进入“正式版结档候选”状态。

### 3.1 构建与基础运行

- [ ] 在根目录执行 `./gradlew build`，确认两个模块都能成功构建
- [ ] 确认产物存在：
  - `GensouMarket-plugin/build/libs/*.jar`
  - `GensouMarket-velocity/build/libs/*.jar`
- [ ] Paper 1.21 + Java 21 + Vault 环境下插件启动成功
- [ ] 首次启动后自动生成：
  - `config.yml`
  - `shop.yml`
  - `recycle.yml`
- [ ] Velocity 端插件能够正常启动并生成 `config.properties`
- [ ] 启动日志中无阻止运行的异常

### 3.2 命令与权限

- [ ] `/gmarket` 可正常打开主菜单
- [ ] `/gmarket help` 帮助文本完整
- [ ] 别名 `/gensoumarket` 与 `/gmarket` 行为一致
- [ ] 普通玩家权限生效：
  - `gensoumarket.use`
  - `gensoumarket.sell`
  - `gensoumarket.buy`
  - `gensoumarket.auction`
  - `gensoumarket.shop`
  - `gensoumarket.recycle`
  - `gensoumarket.trade`
- [ ] 管理员权限生效：
  - `gensoumarket.admin.reload`
  - `gensoumarket.admin.shop.edit`
  - `gensoumarket.admin.recycle.edit`

### 3.3 全球市场

- [ ] 手持正常物品可成功上架
- [ ] 上架税扣除正确
- [ ] 上架后主手物品被移除
- [ ] 上架成功后：
  - GUI 中可见
  - `/gmarket my` 可见
  - ID 显示正常
- [ ] 空手上架会被拒绝
- [ ] 非法价格会被拒绝
- [ ] 上架数量达到上限时会回滚物品与税金
- [ ] 其他玩家购买成功后：
  - 买家只被扣一次钱
  - 买家只获得一次物品
  - 卖家只收到一次税后收入或邮箱补偿
- [ ] 玩家不能购买自己的商品
- [ ] 余额不足时购买失败，状态不会异常
- [ ] 主动下架时：
  - 只能本人或管理员操作
  - 物品返还正确
  - 列表状态更新正确
- [ ] 自然过期时：
  - 物品进入邮箱
  - 卖家收到提示
  - `/gmarket collect` 可领取

### 3.4 拍卖行

- [ ] 可以正常创建拍卖
- [ ] 起拍价必须大于 `0`
- [ ] 拍卖时长受最小/最大时长限制
- [ ] 上架税与拍卖手续费扣除正确
- [ ] 创建后拍卖列表与详情页都可正常查看
- [ ] 非法出价被正确拦截：
  - 低于起拍价
  - 低于或等于当前价
  - 自己竞拍自己的拍卖
- [ ] 成功竞拍后：
  - 新竞拍者扣款正确
  - 前一位竞拍者即时退款或转邮箱
  - 卖家收到提示
- [ ] 拍卖结束后：
  - 仅结算一次
  - 卖家只收一次钱
  - 赢家只收一次物品
- [ ] 无人竞拍时：
  - 物品返还或入邮箱
  - 提示正确
- [ ] 卖家取消拍卖时：
  - 手续费不退
  - 竞拍者退款正确
  - 卖家物品返还正确
- [ ] 服务器重启后：
  - 活跃拍卖定时恢复正常
  - 不发生重复结算

### 3.5 服务器商店

- [ ] `/gmarket shop` 可打开 GUI
- [ ] GUI 左键购买 `1` 个正常
- [ ] GUI Shift 购买 `64` 个正常
- [ ] `/gmarket shop buy <物品ID> [数量]` 正常
- [ ] 扣款金额正确
- [ ] 背包满时会掉落到脚下
- [ ] 余额不足时购买失败提示正确
- [ ] `/gmarket shop prices` 展示价格正确

### 3.6 回收站

- [ ] `/gmarket recycle` 可打开 GUI
- [ ] GUI 左键回收 `1` 个正常
- [ ] GUI 右键回收 `1` 组正常
- [ ] `/gmarket recycle sell [数量]` 正常
- [ ] 不在回收列表中的物品会被拒绝
- [ ] 动态价格计算与实际到账一致
- [ ] lore 每秒刷新正常
- [ ] 涨跌百分比显示正常
- [ ] 回收成功后物品数量扣减正确

### 3.7 管理员配置编辑

- [ ] `/gmarket shop add <价格>` 手持添加商品正常
- [ ] `/gmarket shop add <物品> <价格>` 指定物品添加商品正常
- [ ] `/gmarket shop remove <物品ID>` 正常
- [ ] `/gmarket shop setprice <物品ID> <买入价>` 正常
- [ ] `/gmarket recycle add <回收价>` 手持添加回收物品正常
- [ ] `/gmarket recycle add <物品> <回收价>` 指定物品添加回收物品正常
- [ ] `/gmarket recycle remove <物品ID>` 正常
- [ ] `/gmarket recycle setprice <物品ID> <回收价>` 正常
- [ ] 修改后配置文件写回正确
- [ ] 重载后配置仍正确
- [ ] 重启后配置仍正确

### 3.8 邮箱系统

- [ ] 玩家登录时若有待领取内容，会收到提醒
- [ ] `/gmarket collect` 可正常领取金币
- [ ] `/gmarket collect` 可正常领取物品
- [ ] 同时含金币和物品时可混合领取
- [ ] 部分领取失败时会保留失败部分
- [ ] 不会出现重复领取

### 3.9 面对面交易

- [ ] 蹲下右键玩家可发起交易请求
- [ ] 接受请求可打开双人交易 GUI
- [ ] 拒绝请求逻辑正常
- [ ] 主动取消请求逻辑正常
- [ ] 请求超时逻辑正常
- [ ] 双方交易栏同步正常
- [ ] 任意一方修改物品后，双方确认状态会重置
- [ ] 双方确认后交易完成且物品交换正确
- [ ] 关闭 GUI 会取消交易并返还物品
- [ ] 玩家离线会取消交易并返还物品
- [ ] 超过配置距离会自动取消交易并返还物品
- [ ] 背包满时溢出物品会掉落

### 3.10 配置重载

- [ ] `/gmarket reload` 可成功执行
- [ ] 重载后市场/拍卖基础功能不受影响
- [ ] 重载后商店配置生效
- [ ] 重载后回收站配置生效
- [ ] 重载后不产生明显异常日志

### 3.11 持久化验证

- [ ] `sqlite` 模式下：
  - 市场数据可持久化
  - 拍卖数据可持久化
  - 邮箱数据可持久化
  - 商店/回收动态数据可持久化
- [ ] `mysql` 模式下同样通过以上验证
- [ ] `yaml` 模式下基础功能可运行
- [ ] 跨服模式下若非 `mysql`，插件应拒绝启动

### 3.12 跨服验证

- [ ] Velocity + 两个 Paper 子服 + 共享 MySQL 架构可正常启动
- [ ] `cluster.enabled=true` 时插件成功进入跨服模式
- [ ] 每个子服 `server-id` 唯一
- [ ] Velocity 与 Paper 端 `channel` 完全一致
- [ ] 后端服可被代理注册与注销

- [ ] 跨服市场场景：
  - A 服上架，B 服可见
  - B 服购买后 A/B 两边状态一致
  - 卖家跨服即时到账或邮箱补偿正确

- [ ] 跨服拍卖场景：
  - A 服创建，B 服可见
  - B 服出价后 A/B 两边价格一致
  - 超价退款跨服正确
  - 到时结算只执行一次

- [ ] 跨服邮箱与远程动作：
  - 远程存款成功
  - 远程发物成功
  - 目标玩家不在线时正确回退到邮箱
  - 背包已满时正确回退到邮箱

- [ ] 跨服 GUI 刷新：
  - 从主菜单进入的市场/拍卖视图刷新正常
  - 从命令入口直接打开的市场/拍卖视图也要单独验证
  - 若行为不一致，必须人工决定是否修正

### 3.13 断线与极端场景

- [ ] 在市场购买关键时刻强制下线，不出现脏状态
- [ ] 在拍卖出价关键时刻强制下线，不出现脏状态
- [ ] 在市场下架关键时刻强制下线，不丢物
- [ ] 在拍卖取消关键时刻强制下线，不丢物不丢钱
- [ ] 多玩家同时抢购同一商品时，仅一人成功
- [ ] 多玩家同时竞拍同一拍卖时，不出现价格覆盖与重复退款

## 4. 结档判定标准

仅当以下条件全部满足时，项目才可结档为正式版：

- [ ] 第 `3` 节全部通过
- [ ] 第 `2.1` 节阻塞级问题已修复，或由负责人书面接受风险
- [ ] 第 `2.2` 节一致性与发布决策项均已拍板
- [ ] 文档版本、构建版本、发布策略已最终确认
- [ ] 仓库工作区清理策略已确认

---

如果后续确认要修，我建议优先顺序如下：

1. 市场购买状态悬空问题
2. 拍卖出价状态悬空与普通回滚覆盖问题
3. 下架/取消拍卖时的离线返还缺口
4. 跨服 GUI 视图注册入口统一
5. 文档与仓库发布元信息收口
