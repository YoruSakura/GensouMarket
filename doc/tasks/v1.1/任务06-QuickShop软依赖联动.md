# 任务 06：QuickShop 软依赖联动

- 状态：`后备/低优先级`
- 优先级：低
- 依赖：`任务01-个人商店一期视图.md`

## 目标

在安装 QuickShop 的服务器中，允许其店铺行为与 GensouMarket 个人商店做软依赖联动。

## 当前定位

- 不进入 1.1 主线
- 只作为后续兼容扩展方向保留

## 实现前必须先定的规则

- 使用 QuickShop 的哪套接口（API）/ 事件
- 玩家确认同步的交互形式
- 一次同步还是长期绑定
- 双向同步的优先级和冲突处理
- 版本兼容边界

## 最小可接受范围

- 未安装 QuickShop 时自动降级
- 安装但版本不兼容时不拖垮主插件
- 同步建立后可追踪来源

## 可能涉及的代码区域

- `build.gradle` 软依赖声明
- `plugin.yml` 的软依赖声明（`softdepend`）
- 个人商店数据模型
- QuickShop 事件桥接层

## 备注

- 当前不建议直接排进开发队列
