# GensouMarket v1.1.2 修复操作文档（Claude 接手用）

## 当前基线

当前分支：`v1.1.2`

已把代码生成完成状态提交到本地 Git：

```text
cabfd68 chore: checkpoint v1.1.2 generated implementation
```

这次提交只是本地 checkpoint，没有推送。后续修复完成后，可以直接用 `git diff cabfd68..HEAD` 或普通 `git diff` 对比修复内容。

当前已验证：

```bash
./gradlew :GensouMarket-plugin:clean :GensouMarket-plugin:build :GensouMarket-velocity:clean :GensouMarket-velocity:build
```

结果：`BUILD SUCCESSFUL`。

注意：编译通过不代表跨服人工测试通过。以下问题需要先修。

## 修复优先级

优先修 P0/P1，再做版本号和 GUI 刷新收口。

1. P0：MySQL 双连接池泄漏。
2. P0：升级版本写入时机错误，可能把失败迁移标成成功。
3. P1：跨服 recycled 购买补偿失败没有日志和兜底。
4. P1：Meta 写入失败被吞，防降级状态不可靠。
5. P2：GUI 异步购买后刷新时机错误。
6. P2：版本号不一致。

## P0-1 MySQL 双连接池泄漏

### 问题

`UpgradeManager.runStorageMigration()` 会在 `storage.initialize()` 前调用：

```java
mysqlStorage.initDataSource();
```

随后 `MySQLStorage.initialize()` 又无条件执行：

```java
dataSource = new HikariDataSource(createHikariConfig());
```

旧连接池没有关闭，MySQL 模式每次启动会泄漏连接和线程。

### 修改位置

- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/storage/MySQLStorage.java`
- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/upgrade/UpgradeManager.java`

### 推荐修法

修改 `MySQLStorage.initialize()`，复用已存在的连接池：

```java
@Override
public void initialize() throws Exception {
    initDataSource();
    try (Connection conn = getConnection()) {
        createTables(conn);
        migrateSchema(conn);
        migrateShopToRecycle(conn);
    }
}
```

要求：

- 不要在 `initialize()` 中无条件 new 第二个 `HikariDataSource`。
- `shutdown()` 仍负责关闭唯一连接池。
- `initDataSource()` 保持幂等。

### 验收

- MySQL 模式启动日志中不应出现两个 Hikari pool 初始化。
- 多次 `/reload` 或重启后 MySQL 连接数不应持续增长。

## P0-2 升级版本写入时机错误

### 问题

当前 `UpgradeManager.runStorageMigration()` 在 `storage.initialize()` 之前执行，并且 v4 -> v5 是 `NoOpMigrationStep`。它会先把 `data-version` 写成 v5，然后 `storage.initialize()` 才真正执行建表、补列和旧数据迁移。

如果 `storage.initialize()` 失败，下次启动会误判数据已经是 v5，升级报告和防降级状态都会失真。

### 修改位置

- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/GensouMarket.java`
- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/upgrade/UpgradeManager.java`
- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/storage/MySQLStorage.java`
- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/storage/SQLiteStorage.java`

### 推荐修法

第一阶段最小安全修法：

1. `runStorageMigration(storage)` 只做：
   - 初始化 meta 表或 `upgrade-state.yml`。
   - 读取当前版本。
   - 防降级检查。
   - 记录待升级状态，但不要写最终 `data-version = 5`。
2. `storage.initialize()` 成功后，再调用新方法：

```java
upgradeManager.markStorageInitialized(storage);
```

3. `markStorageInitialized(...)` 再写：
   - `data-version = CURRENT_DATA_VERSION`
   - `last-plugin-version`
   - `last-migration-id`
   - `upgrade-report-latest.yml`

更彻底修法：

- 把 `createTables`、`migrateSchema`、`migrateShopToRecycle` 真正纳入 `MigrationStep`。
- 但不要为了这次修复做大规模重排，先确保版本写入不早于真实 schema 成功。

### 强制要求

- 迁移失败或 `storage.initialize()` 失败，不得写最终版本。
- 迁移失败后业务模块不得启动。
- 报告里要能看出失败发生在哪一步。

### 验收

手动制造 MySQL 初始化失败，确认：

- 插件禁用。
- `gensoumarket_meta.data-version` 不会被提前写成 v5。
- `upgrade-report-latest.yml` 显示失败。

## P1-1 recycled 购买补偿失败无日志

### 问题

跨服 recycled 购买中，MySQL 条件扣减成功后，如果玩家离线、余额不足或扣款失败，会调用：

```java
storage.addRecycleStockDelta(recycleSource, amount, 0, ...)
```

但没有检查返回值。补偿失败会永久少库存。

### 修改位置

- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/shop/ShopManager.java`

### 推荐修法

抽出补偿方法：

```java
private void compensateRecycleStockAsync(RecycleItem recycleSource,
                                         int amount,
                                         UUID playerUuid,
                                         String action,
                                         String reason)
```

要求：

- 异步执行 `addRecycleStockDelta(...)`。
- 检查返回值。
- 返回 false 时写 `SEVERE` 日志。
- 日志必须包含：
  - `itemId`
  - `amount`
  - `playerUuid`
  - `action`
  - `reason`

建议日志格式：

```text
[Shop] 回流库存补偿失败！item=<id> amount=<amount> player=<uuid> action=shop-buy reason=<reason>
```

### 验收

模拟补偿失败时日志必须是 `SEVERE`，不能只静默失败。

## P1-2 Meta 写入失败被吞

### 问题

`SqlMetaStore.set(...)` 和 `YamlMetaStore.save()` 捕获异常后只打印 warning。`UpgradeManager` 会继续认为版本写入成功。

版本元数据是防降级和迁移幂等的关键状态，不能吞失败。

### 修改位置

- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/upgrade/meta/MetaStore.java`
- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/upgrade/meta/SqlMetaStore.java`
- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/upgrade/meta/YamlMetaStore.java`
- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/upgrade/UpgradeManager.java`

### 推荐修法

把 `MetaStore.set(...)` / `setInt(...)` 改为可抛异常，或新增严格写入方法：

```java
void setStrict(String key, String value) throws Exception;
void setIntStrict(String key, int value) throws Exception;
```

`UpgradeManager` 中写关键版本字段时必须用 strict 方法。

关键字段：

- `data-version`
- `config-version`
- `last-plugin-version`
- `last-migration-id`

### 验收

模拟 `upgrade-state.yml` 不可写或 MySQL meta 表写入失败：

- 插件必须禁用。
- 不得继续启动业务模块。
- 日志要明确指出 meta 写入失败。

## P2-1 GUI 异步购买刷新时机错误

### 问题

`GuiListener.handleShop(...)` 在确认阶段调用：

```java
plugin.getShopManager().buyFromShop(player, shopItem.getId(), amount);
ShopGui.openShop(plugin, player, page);
```

但跨服 recycled 购买现在是异步流程。GUI 会在购买完成前立即刷新，玩家看到旧库存/旧价格；购买失败时也不会按最终结果刷新。

### 修改位置

- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/gui/GuiListener.java`
- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/shop/ShopManager.java`

### 推荐修法

给 `buyFromShop` 增加可选完成回调：

```java
public void buyFromShop(Player player, String itemId, int amount, Runnable onComplete)
```

保留旧重载：

```java
public void buyFromShop(Player player, String itemId, int amount) {
    buyFromShop(player, itemId, amount, null);
}
```

要求：

- 同步购买路径结束后执行回调。
- 异步 recycled 路径成功或失败后，在主线程执行回调。
- GUI 确认阶段改为把 `ShopGui.openShop(plugin, player, page)` 放进回调。
- 玩家离线时不要打开 GUI。

### 验收

跨服 recycled 商品确认购买后：

- 成功时刷新后的 GUI 显示扣减后的库存。
- 失败时提示后刷新，显示当前库存。

## P2-2 版本号不一致

### 问题

当前：

- `VersionRegistry.PLUGIN_VERSION = "1.1.2"`
- `GensouMarket-plugin/build.gradle version = '1.1.1'`
- `GensouMarket-velocity/build.gradle version = '1.1.1'`

构建 jar 和 `plugin.yml` 会显示 1.1.1，但 upgrade report 写 1.1.2。

### 修改位置

- `GensouMarket-plugin/build.gradle`
- `GensouMarket-velocity/build.gradle`
- `GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/upgrade/VersionRegistry.java`

### 推荐修法

如果当前仍是开发版，统一为：

```text
1.1.2-dev
```

如果准备发布，再统一为：

```text
1.1.2
```

不要出现 jar 版本、plugin.yml 版本、upgrade report 版本不一致。

### 验收

构建后检查：

```bash
ls GensouMarket-plugin/build/libs/
ls GensouMarket-velocity/build/libs/
```

jar 文件名和 `upgrade-report-latest.yml` 的 `plugin-version` 应一致。

## 修复后必须运行

```bash
./gradlew :GensouMarket-plugin:clean :GensouMarket-plugin:build :GensouMarket-velocity:clean :GensouMarket-velocity:build
```

再做静态检查：

```bash
rg -n "new HikariDataSource|initDataSource|data-version|last-migration-id" GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket
rg -n "addRecycleStockDelta\\(" GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/shop/ShopManager.java
rg -n "version =|PLUGIN_VERSION" GensouMarket-plugin/build.gradle GensouMarket-velocity/build.gradle GensouMarket-plugin/src/main/java/net/scarletphantasy/gensouMarket/upgrade/VersionRegistry.java
```

## 跨服人工测试重点

至少验证：

1. A 服回收 64 个物品，B 服 recycled 商品库存增加。
2. B 服购买 10 个 recycled 商品，A 服库存减少。
3. 两服同时购买最后 1 个库存，最多一个成功。
4. B 服持有旧库存快照时执行 `/gmarket reload`，MySQL 不回退。
5. B 服持有旧库存快照时正常关闭，MySQL 不回退。
6. Velocity 断开导致事件丢失后，定期校准能恢复库存。
7. MySQL 初始化失败时不会提前写 `data-version = 5`。
8. MySQL 连接池不会重复创建。

## 注意事项

- 不要推送。
- 不要提交 `CODEX_SESSION_SUMMARY.md`。
- 不要把 `doc/` 加入 Git。
- 不要把 Velocity 做成业务状态持有者。
- `RECYCLE_STOCK_SYNC` 接收端仍然只能改本地内存，不能写 MySQL。
- recycled 商品跨服购买仍必须以 MySQL `WHERE recycled_stock >= ?` 条件扣减为最终凭证。
