# GensouMarket v1.1.2 核心人工测试文档

## 测试目标

本测试覆盖 v1.1.2 正式版必须确认的核心行为：

- 插件能正常启动，版本正确。
- 跨服 recycled 回流库存能同步。
- reload / 重启不会让库存回退。
- 服务器商店 GUI 交互符合预期。
- v1.1.1 已修复的关键问题没有明显回退。

## 测试对象

测试 jar：

```text
GensouMarket-plugin/build/libs/GensouMarket-plugin-1.1.2.jar
GensouMarket-velocity/build/libs/GensouMarket-velocity-1.1.2.jar
```

当前 SHA256：

```text
GensouMarket-plugin-1.1.2.jar
37a93df3cbb4d34555f64d754429a913e2aef79be5fe40b02bf99935a474a45f

GensouMarket-velocity-1.1.2.jar
c0fd61e4babbef9a9c19421fc1750d05660e3cf543dfa5558281304f966681c3
```

构建命令：

```bash
./gradlew :GensouMarket-plugin:clean :GensouMarket-plugin:build :GensouMarket-velocity:clean :GensouMarket-velocity:build
```

## 测试环境

最低需要：

- Velocity 代理端 1 个。
- Paper 子服 2 个，记为 A 服、B 服。
- 两个子服都安装 `GensouMarket-plugin-1.1.2.jar`。
- Velocity 安装 `GensouMarket-velocity-1.1.2.jar`。
- 两个子服连接同一个 MySQL。
- 两个子服 `cluster.enabled=true`，且 `cluster.server-id` 不同。
- Vault 与经济插件可用。

测试商品可以使用现有 recycled 商品；如果没有，使用配置文件正常添加一个 recycled 商品即可，不需要直接写数据库。

## 测试记录表

| 编号 | 项目 | 结果 | 备注 |
| --- | --- | --- | --- |
| T-112-01 | 构建与启动 | 未测 |  |
| T-112-02 | 跨服回收同步 | 未测 |  |
| T-112-03 | 跨服购买同步 | 未测 |  |
| T-112-04 | reload 后库存不回退 | 未测 |  |
| T-112-05 | 重启后库存不回退 | 未测 |  |
| T-112-06 | 缺货与购买 GUI | 未测 |  |
| T-112-07 | GUI 文案 | 未测 |  |
| T-112-08 | 基础回归 | 未测 |  |

## T-112-01 构建与启动

步骤：

1. 执行构建命令。
2. 将两个 `1.1.2` jar 分别放入 Paper 和 Velocity。
3. 启动 Velocity、A 服、B 服。
4. 查看控制台日志。

通过标准：

- 构建成功。
- 两个 jar 文件名都是 `1.1.2`。
- A 服、B 服插件都正常启用。
- Velocity 无插件消息解码错误。
- 跨服模式下如果不是 MySQL，Paper 插件会拒绝启用。

## T-112-02 跨服回收同步

步骤：

1. B 服打开 `/gmarket shop`，记录某个 recycled 商品的库存和售价。
2. A 服玩家准备对应材料。
3. A 服执行回收，例如：

```text
/gmarket recycle sell 64
```

4. B 服重新打开 `/gmarket shop`。

通过标准：

- B 服同一 recycled 商品库存增加。
- B 服售价随回流库存变化而刷新。
- Velocity 无解码失败日志。

## T-112-03 跨服购买同步

步骤：

1. 确认 recycled 商品有库存。
2. B 服购买该 recycled 商品，例如：

```text
/gmarket shop buy <商品ID> 10
```

3. A 服重新打开 `/gmarket shop`。

通过标准：

- A 服同一商品库存减少。
- A 服售价按新库存刷新。
- B 服购买成功后扣款并获得物品。
- 如果库存不足，只提示失败，不扣款、不发物品。

## T-112-04 reload 后库存不回退

步骤：

1. A 服先通过回收或购买改变 recycled 商品库存。
2. B 服执行：

```text
/gmarket reload
```

3. A 服、B 服分别重新打开 `/gmarket shop`。

通过标准：

- A/B 两服展示的 recycled 库存一致。
- B 服 reload 不会把库存恢复到旧值。
- 控制台无保存回收数据相关严重错误。

## T-112-05 重启后库存不回退

步骤：

1. A 服通过回收或购买改变 recycled 商品库存。
2. 正常关闭并重启 B 服。
3. B 服重新打开 `/gmarket shop`。

通过标准：

- B 服重启后显示的是最新库存。
- 库存不会回退到重启前旧快照。
- 控制台无升级或存储初始化失败日志。

## T-112-06 缺货与购买 GUI

步骤：

1. 找一个库存为 0 的 fixed limited 或 recycled 商品。
2. 玩家点击该缺货商品。
3. 找一个有库存的 recycled 商品，在 GUI 中点击一次，再点击第二次确认购买。
4. 再测试一次：第二次确认后立刻关闭 GUI 或切换到其他菜单。

通过标准：

- 缺货商品点击一次就提示缺货。
- 缺货商品不会进入“再次点击确认购买”。
- 有库存商品购买成功后，玩家扣款并获得物品。
- 玩家仍停留在原商店页时，购买完成后 GUI 会刷新。
- 玩家已经关闭或切换 GUI 时，不会被强制拉回商店页。

## T-112-07 GUI 文案

步骤：

1. 打开 `/gmarket` 主菜单。
2. 查看服务器商店入口。
3. 打开 `/gmarket shop`，查看 fixed unlimited、fixed limited、recycled 商品 lore。

通过标准：

- 服务器商店入口不再出现“价格固定”。
- 商品 lore 不显示完整倍率计算细节。
- 商品仍显示价格、库存和售价来源。
- recycled 商品显示“售价来源：回流库存”。

## T-112-08 基础回归

步骤：

1. 执行 `/gmarket reload`。
2. 打开 `/gmarket recycle`，回收一个普通物品。
3. 打开 `/gmarket shop`，购买 fixed unlimited 商品。
4. 如果有 fixed limited 商品，购买一次后重开商店查看库存。
5. 观察 Velocity 和 Paper 控制台日志。

通过标准：

- `/gmarket reload` 不报错。
- 回收能正常入账。
- fixed unlimited 商品能正常购买。
- fixed limited 商品购买后库存减少，并在重开 GUI 后保持。
- `PRESSURE_SYNC` 没有解码错误。
- 没有新的 `SEVERE` 错误，尤其没有回流库存补偿失败日志。

## 测试失败记录格式

```text
测试编号:
服务器:
操作步骤:
预期结果:
实际结果:
相关日志:
是否可复现:
```

## 通过结论

建议全部满足后再进入发布流程：

- T-112-01 到 T-112-08 全部通过。
- A/B 两服 recycled 库存能随回收和购买同步。
- reload 和重启后库存不回退。
- GUI 文案和确认流程符合预期。
- Velocity 和 Paper 控制台没有新的严重错误。
