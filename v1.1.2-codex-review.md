# GensouMarket v1.1.2 代码生成与实现总结 (Codex 审查报告)

## 1. 版本目标与背景

本次更新的核心目标是解决 **跨服（Velocity + 多 Paper）环境下 recycled（回流）商品库存的一致性问题**。
在 v1.1.1 中，跨服回收和购买依赖本地内存计算并定时写入数据库，导致在多子服并发场景下出现严重的库存覆盖、超卖以及更新不同步问题。

v1.1.2 采用 **“MySQL 最终权威 + Velocity 实时事件广播 + 乐观内存预检”** 的混合架构彻底解决了这一问题，同时不影响单服模式的性能，并增加了稳健的数据库升级与防降级框架。

---

## 2. 核心模块实现细节

整个代码生成过程严格按照实现文档，拆分为 6 个模块稳步推进：

### 模块 01：跨服回流库存同步协议
为了在多子服间实时同步回收和购买导致的库存变动，扩展了原有的 Plugin Messaging 协议。
- **协议新增**：在 `PacketType` 中定义了 `RECYCLE_STOCK_SYNC(0x71)` 协议。
- **消息路由**：Velocity 端新增对该包的解析，并通过 `handleBroadcast` 将增量广播至集群中的其他所有存活子服。
- **本地应用**：Paper 端 `RemoteActionHandler` 解析广播后，调用 `RecycleManager.applyRemoteRecycleStockDelta()` 将其他服的库存增量（或负增量）合并到本地内存，确保 UI 展示的实时性。

### 模块 02：回流库存存储原子性
摒弃了原来的 “读取内存 -> 计算 -> 全量覆盖” 逻辑，转而使用 MySQL 的原子操作。
- **防止负库存**：实现 `addRecycleStockDelta`，使用 `UPDATE ... SET recycled_stock = GREATEST(0, recycled_stock + ?)` 确保回收或补偿时库存绝对安全。
- **防止超卖 (核心)**：实现 `consumeRecycleStockIfEnough`，利用 `UPDATE ... WHERE recycled_stock >= ?` 依靠数据库的行锁在并发购买时保证最多只有一个事务能成功扣减。
- **分离运行时状态**：新增 `saveRecycleDefinitionData`，使得集群模式下管理员更改配置（如价格）时只保存定义数据，坚决不覆盖库存等运行时状态。

### 模块 03：业务流程与补偿机制重构
重构了购买和回收的业务闭环，使其支持全新的原子跨服流。
- **回收逻辑 (`RecycleManager.recycleItem`)**：
  改为 **主线程执行** `Vault扣除 -> MySQL 原子增量 -> 内存更新 -> 发送跨服同步事件`。
  如果 MySQL 写入失败，实现了立即对玩家执行 Vault 退款并返还物品的补偿逻辑。
- **购买逻辑 (`ShopManager.buyRecycledCluster`)**：
  采用 **异步条件扣减**：先切异步线程请求 MySQL 尝试扣减库存。如果扣减成功回到主线程，进行最终的玩家余额扣除和物品发放；如果中途（玩家离线、余额不足、背包问题）失败，则立刻发起异步的库存补偿（补回被锁定的库存），有效解决竞争抢购。
- **定时校准**：通过 `scheduleRecycleRuntimeCalibration()`，每 3 分钟从 MySQL 读取权威库存强制校正本地内存，防止消息丢失引起的长期脑裂。

### 模块 04：升级与降级框架
为插件引入了正规的数据库结构管理。
- **版本控制**：使用 `gensoumarket_meta` 表（SQL）和 `upgrade-state.yml`（配置侧）分离追踪数据与配置的版本。
- **安全检查**：在 `GensouMarket.onEnable()` 中引入了严苛的两阶段检查。一旦发现现存数据版本高于当前插件支持版本（即试图危险降级），立刻写入报告 `upgrade-report-latest.yml` 并强制禁用插件，防止数据损毁。
- **平滑迁移**：统一了 `MigrationStep` 接口，原有的 schema 修改与配置补齐无缝衔接至该框架下。

### 模块 05：体验优化与 GUI 文案
优化了玩家的交互体验。
- **即时拦截**：在 `GuiListener` 点击事件中加入库存预检。一旦缺货，不再进入二次确认流程（记录 `pendingConfirmations`），而是直接弹出缺货提示。
- **UI 精简**：去除了普通玩家在 `ShopGui` 中看到的各种复杂倍率（经济、库存、防套利）调试信息，统一以“售价来源：基础价格/库存/回流库存”展示。主菜单也移除了误导性的“价格固定”文案。

### 模块 06：验证测试与收口
通过 Gradle 执行了双端的 Clean Build 并通过。
进行了全面的静态代码走查：
- 确认没有跨服保存覆写漏洞。
- 确认 Bukkit Scheduler 的多线程规范（无异步操作原生 API）。
- 确认 v1.1.1 核心机制未发生回归。

---

## 3. 架构结论

通过本次 v1.1.2 改造，GensouMarket 在集群下的数据流向变得清晰：
1. **写权威**：MySQL 数据库（依赖其原子修改保证一致性）。
2. **读权威**：Paper 本地内存（保证无延迟的 GUI 体验）。
3. **最终一致性保证**：Velocity 的事件多播 (毫秒级同步) + 周期性的 MySQL 定时校准 (分钟级兜底)。

所有目标达成，代码具备高并发可用性与严格的事务安全性。
